# CSV天气数据文件选择功能

## 功能概述

现在风机集群模拟器支持选择不同的CSV文件作为天气数据源，提供更灵活的数据配置选项。

## 新增功能

### 1. 启动时选择天气文件

运行模拟器时，程序会提示选择天气数据文件：

```
📁 选择天气数据文件:
1. 使用默认文件 (weather.csv)
2. 指定CSV文件路径
请选择 (1/2):
```

### 2. 代码中指定天气文件

在代码中直接指定天气数据文件：

```python
# 使用自定义天气文件
simulator = WindFarmClusterSimulator(
    turbine_count=20,
    enable_mqtt=False,
    weather_file="my_custom_weather.csv"  # 指定CSV文件
)
```

### 3. 运行期间动态更换天气文件

模拟运行时可输入命令动态更换天气数据：

```
💬 输入命令 (change/info/help): change new_weather.csv
💬 输入命令 (change/info/help): info
💬 输入命令 (change/info/help): help
```

## 可用命令

| 命令 | 功能 | 示例 |
|-----|------|------|
| `change <文件路径>` | 更换天气数据文件 | `change data/weather2.csv` |
| `info` | 显示当前天气文件信息 | `info` |
| `help` | 显示帮助信息 | `help` |
| `Ctrl+C` | 停止模拟 | - |

## CSV文件格式要求

天气数据CSV文件必须符合以下格式：

1. **多级列标题** - 包含高度信息
2. **必需列**：
   - `wind_speed (80)` - 80米高度风速
   - `temperature (10)` - 10米高度温度
   - `pressure (0)` - 地面气压
3. **时间索引列** - 作为第一列

示例格式：
```csv
,wind_speed,temperature,pressure
,80,10,0
2020-01-01 00:00:00,5.2,273.15,101325
2020-01-01 01:00:00,6.1,272.98,101320
...
```

## 错误处理

- **文件不存在**：自动回退到默认weather.csv文件
- **格式错误**：显示错误信息并保持原文件
- **加载失败**：提供详细错误信息

## 使用示例

### 启动时选择文件

```bash
python wind_farm_cluster_simulator.py
# 按提示选择天气数据文件
```

### 快速测试功能

```bash
python test_weather_selection.py
# 运行功能测试
```

### 在Web应用中使用

Web应用的CSV上传功能会自动调用文件更换方法，实现实时数据更新。

## 功能优势

1. **灵活性** - 支持多种天气数据源
2. **实时性** - 运行期间可动态更换数据
3. **稳定性** - 完善的错误处理机制
4. **兼容性** - 保持向后兼容，默认行为不变
5. **用户友好** - 清晰的命令界面和反馈信息

## 注意事项

- 文件路径可以是相对路径或绝对路径
- 程序会在当前目录下自动查找文件
- 更换文件后会重置数据索引到开头
- MQTT发布会继续使用新的天气数据 